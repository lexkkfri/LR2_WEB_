# Лабораторна робота №2-4 з дисципліни "Веб-орієнтована розробка ПЗ"
Виконала студентка групи КІ-33 Коростіль Олександра

## Робота з JS:
js/db.js - Сервіс Бази Даних (IndexedDB). Центральний файл для всіх операцій із даними: авторизація, реєстрація, зберігання поточних To-Do/Notes, архівування днів та керування дедлайнами. Визначає чотири сховища: users, user_data, archived_days, deadlines.
js/utils.js -	Утиліти Валідації. Містить допоміжні функції для регулярних виразів (валидація email), а також функції showError, showSuccess та clearErrors, що відповідають за візуальне відображення валідації на сторінках авторизації.
js/global.js	- Глобальний Контролер UI. Ініціалізує загальні елементи: керування випадаючим меню аватара, перемикання світлої/темної теми (збереження стану в localStorage) та логіка виходу з системи (logout).
js/login.js	- Обробка форми входу. Викликає dbService.loginUser та керує перенаправленням і відображенням помилок автентифікації.
js/register.js - 	Обробка форми реєстрації. Містить логіку валідації полів (імені, email, паролів) та використовує dbService.registerUser для створення облікового запису.
js/app.js	- Головний робочий простір. Керує: To-Do List (додавання, завершення, видалення), автозбереженням щоденних нотаток, логікою архівування дня (dbService.archiveDay) та відображенням архівів.
js/deadline-tracker.js	-	Керує системою дедлайнів. Включає: відображення статистики, форми додавання/редагування завдань, рендеринг календаря з позначками дедлайнів, а також логіку модальних вікон.
js/timer.js	- Реалізація Pomodoro-таймера. Керує режимами Study/Rest, перемиканням сесій, паузою/стартом та оновленням відображення часу та CSS-класів режиму.

## Завдання лабораторної роботи №4:
1. Усі скрипти підключені до відповідних сторінок через <script src="./js/app.js"></script>.
2. Приклад реалізації роботи з DOM:
   - У файлі deadline-tracker.js при ініціалізації додатку відбувається пошук усіх елементів з класом .avatar, щоб встановити в них ініціали користувача. Це пряма маніпуляція елементом, знайденим за класом, через зміну його властивості innerText.
   - Приклад динамічного додавання контенту: створення елементів списку справ listEl.appendChild(li);
3. Події та обробники:
  - реалізована зміна теми (перемикання на клас dark-theme)
  - На головній сторінці використовується обробник input для поля нотаток (notesInputEl.addEventListener('input', ...)), який спрацьовує при введенні тексту з клавіатури, ініціюючи автозбереження через setTimeout. Це є прикладом відстеження події клавіатури, яка викликає динамічну функцію.
4. Робота з формами та валідацією:
  - Реалізована заборона стандартної відправки event.preventDefault().
  - Зчитування даних з усіх полів: Зчитуються значення usernameInput, emailInput, passwordInput, confirmPasswordInput та зберігаються у змінних.
  - У разі успішної реєстрації, дані (username, email, password) використовуються для виклику функції dbService.registerUser(userData), а також відображається повідомлення alert('Account successfully created!');
  - Реалізована логіка перевірок:
    1. Перевірка usernameInput.value.trim().length < 3.
    2. Виклик допоміжної функції validateEmail(emailInput.value), яка використовує регулярний вираз для перевірки коректності email. Логіка функції validateEmail знаходиться у utils.js.
    3. Валідація пароля (мінімум 6 символів) та підтвердження пароля (порівняння).
  - Висвітлення помилок:
    1. Використовується функція showError(input, message), яка додає CSS-клас 'error' до поля вводу. Стиль цього класу визначений у auth.css (червона рамка).
    2. Функція showError створює <div> з класом .error-message і вставляє повідомлення про помилку під відповідним полем форми.
  - Успішна валідація:
    1. Форма не очищується після успішного входу, оскільки після реєстрації/входу відбувається перенаправлення на index.html. Якщо перенаправлення не відбудеться (наприклад, для форми To-Do), функція handleFormSuccess у utils.js містить команду form.reset();.
    2. Після успішної реєстрації викликається alert('Account successfully created!');
5. Робота з локальним сховищем:
   - При перемиканні теми стан записується в localStorage. localStorage.setItem('ordo_theme', isDark ? 'dark' : 'light');
   - localStorage використовується для зберігання об'єкта сесії користувача (ordo_user) після успішного входу. Цей об'єкт містить ім'я користувача (username) та його email.
   - Збереження списку завдань (currentTodos) та нотаток (daily-notes-input) за допомогою IndexedDB

## Робота з CSS стилями:
Стилі організовані за модульним принципом, де кожна функціональна частина винесена в окремий файл (base.css, layout.css, components.css, pages.css, auth.css), а потім об'єднана через директиву @import у головному файлі styles.css. Це забезпечує зручність керування та масштабування. Для централізованого контролю над дизайном активно використовуються CSS-змінні (Custom Properties), що дозволяють легко керувати кольоровою палітрою та типографікою проєкту.

Для створення адаптивного та гнучкого макету в проєкті широко застосовуються ключові сучасні технології розташування елементів. CSS Flexbox використовується для побудови одновимірних структур, таких як навігаційні меню, центрування елементів та керування перенесенням кнопок на мобільних пристроях (наприклад, у секції таймера). CSS Grid застосовано для створення складніших двовимірних сіток, включаючи макет головної сторінки (content-grid) та секції "Features" на лендінгу.

Адаптивність забезпечується за допомогою медіазапитів (@media), які дозволяють гнучко перебудовувати макет сторінки (наприклад, перехід від колонок до стовпців на мобільних пристроях, приховування аватару в мобільній навігації). Це гарантує коректне відображення на всіх типах пристроїв.

Впроваджено також базову клієнтську HTML5-валідацію з використанням псевдокласів CSS (:invalid, :valid) для візуального підсвічування рамок полів введення. Нарешті, для забезпечення доступності всіх інтерактивних елементів використовується стилізація :focus акцентним зеленим кольором.

## Опис проєкту:
Ordo - це сучасний веб-сервіс для відстеження щоденного прогресу в роботі та навчанні. Допомагає організовувати робочий процес, контролювати дедлайни та підтримувати особисту продуктивність.

### Основні можливості
- Щоденник та нотатки
- Списки справ та дедлайни
- Таймер концентрації (Pomodoro)
- Відстеження прогресy та звичок

Сервіс призначений для студентів, офісних працівників та фрілансерів, які хочуть ефективно керувати своїм часом та завданнями.

## Структура проєкту:
ordo/
│
├── 📁 css/                    # Стилі проекту
│
├── 📁 images/                 # Графічні ресурси
│
├── 📁 js/                     # JavaScript файли
│
├── 📄 index.html              # Головна сторінка (Home)
├── 📄 description.html        # Описова сторінка продукту
├── 📄 deadline-tracker.html   # Трекер дедлайнів
├── 📄 timer.html              # Сторінка таймера
├── 📄 login.html              # Сторінка входу
└── 📄 register.html           # Сторінка реєстрації
